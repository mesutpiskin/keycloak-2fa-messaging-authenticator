# Local Testing

## Build

```bash
mvn -B verify
```

## Validate without sending real messages

Enable `simulation.mode` in the authenticator configuration to verify flow wiring before real delivery.
